# 瞰维智管官方网站需求基线

> 本文持续记录已确认的官网需求。第 1～8 节保留 Stage 0 工程基线；第 9 节起记录 Stage 1 已锁定要求。Stage 1 视觉变量优先于 Stage 0 的临时配色。

## 0. 2026-08-22 最新完整能力确认（覆盖旧状态结论）

- 产品负责人确认 `app/data/product-capabilities.js` 中 206 项能力均已完善，公开 `availability` 统一为 `available`。
- 公开页面不再使用旧“已上线／按项目配置／评估接入／规划中”四状态；交付差异独立记录为 `standard`、`configuration`、`integration`、`private-deployment`。
- 缺少独立页面截图只表示公开视觉证据待补，不得降低产品能力完成状态。
- 首页旧六项能力展示整体删除，改为四大经营结果、三层架构、三条业务闭环、七大能力域、九个真实系统模块、206 项能力查看器、六岗位价值、AI 与技术底座。
- AI 招商匹配、AI 催缴、OCR、异常告警、租金分析、经营助手、经营问答和票据检查均作为当前完整产品能力展示，同时保留人工确认和数据最小化边界。
- 软硬件、接口和云端私有化部署能力已经完善；具体启用方式根据客户设备型号、协议、网络、服务器条件和实施范围确定。
- 本节为当前有效口径。本文后续保留的旧“六项能力”和旧四状态段落仅作历史阶段记录，不再作为官网公开文案和产品状态依据。

## 1. 项目身份与隔离原则

- 项目名称：瞰维智管官方网站。
- 官网规划域名：`yizuw.org`。
- 既有业务系统地址：`yizuw.cn`。
- 官网必须作为独立工程和独立 Git 仓库建设。
- 官网开发、构建和测试不得访问、修改或影响 `yizuw.cn` 生产系统。
- 本阶段不部署、不修改 DNS、不操作生产数据。

## 2. 技术约束

- 前端框架使用 Vue/Nuxt。
- 业务代码和配置使用 JavaScript，不使用 TypeScript。
- 包管理器使用 npm。
- 项目应支持本地开发、生产构建、预览和代码检查。

## 3. 第一阶段范围

第一阶段只建立可运行、可检查、可继续演进的官网项目基线，不开始最终视觉设计。

### 3.1 页面路由

| 页面 | 路由 | 当前交付 |
| --- | --- | --- |
| 首页 | `/` | 临时运行验证页 |
| 产品能力 | `/products` | 路由骨架 |
| 解决方案 | `/solutions` | 路由骨架 |
| 客户案例 | `/cases` | 路由骨架 |
| 关于我们 | `/about` | 路由骨架 |
| 预约演示 | `/demo` | 路由骨架 |
| 隐私政策 | `/privacy` | 路由骨架 |

所有路由必须能够在桌面端和移动端正常打开，并包含基础页面标题与描述信息。

### 3.2 设计基线

全局设计变量必须至少包含以下品牌色：

- 深蓝：`#0D2A45`，用于主色、导航和重点文字。
- 砖红：`#A94B3C`，用于强调和行动入口。
- 暖白：`#F6F1E8`，用于页面背景。

同时建立字体、间距、圆角、阴影和内容宽度等基础变量，供后续视觉设计统一使用。当前样式仅用于验证布局和响应式能力，不代表最终视觉方案。

### 3.3 素材目录

公开静态素材统一放在 `public/assets/`：

- `public/assets/images/`：通用图片。
- `public/assets/logo/`：Logo 与品牌标识。
- `public/assets/cases/`：客户案例素材。
- `public/assets/wecom-qrcode/`：企业微信二维码。

当前阶段只创建目录说明，不放入真实客户资料或企业微信二维码。

### 3.4 工程能力

- `npm run dev`：启动本地开发服务。
- `npm run build`：生成生产构建。
- `npm run preview`：预览生产构建。
- `npm run lint`：执行代码检查。
- `npm run check`：顺序执行代码检查和构建。
- 使用环境变量配置站点 URL 和搜索引擎收录开关。

## 4. 基础 SEO

- 文档语言设置为简体中文。
- 提供全局站点名称、描述、主题色和 Open Graph 基础信息。
- 每个页面提供独立标题、描述、Canonical URL、Open Graph 和 Twitter Card 元信息。
- 阶段环境默认输出 `noindex, nofollow`，只有将 `NUXT_PUBLIC_INDEXABLE=true` 显式注入经过审批的正式环境后才允许收录。
- 本阶段不接入统计、广告或第三方追踪脚本。

## 5. 响应式与可访问性基线

- 桌面端和移动端均能访问所有页面。
- 页面采用语义化的页眉、导航、主体和页脚结构。
- 键盘焦点可见，颜色变量保留足够清晰的视觉层级。
- 内容容器和导航在窄屏下自动重排，不产生横向滚动。

## 6. 安全与数据要求

- 仓库不得包含真实系统账号、密码、密钥、Token 或生产配置。
- 仓库不得包含真实经营数据、未授权客户信息或真实客户案例素材。
- `.env` 文件不纳入 Git，仓库只提供无敏感信息的 `.env.example`。
- 官网基线不调用 `yizuw.cn` API，不连接生产数据库。

## 7. 明确不在本阶段内

- 最终品牌视觉、完整文案和正式素材制作。
- 表单提交后端、CRM、短信、邮件或企业微信集成。
- 用户登录、业务系统单点登录或生产 API 接入。
- 域名解析、证书配置、服务器配置、部署与上线。
- 生产数据迁移、读取或修改。

## 8. 第一阶段验收标准

- 本地项目能够正常启动。
- 七个页面路由全部能够访问。
- 代码检查通过。
- 生产构建无错误。
- 桌面端和移动端均能正常访问，无明显横向溢出。
- 不包含真实系统账号、密码和经营数据。
- 未访问或修改 `yizuw.cn` 生产系统。
- 未部署上线，未修改 DNS。
- 验收报告最终输出 `STAGE_0=PASS`；如未通过，应输出具体失败原因。

## 9. Stage 1：品牌与产品定位

- 品牌名称：瞰维智管。
- 产品定位：面向工业园区的经营管理系统。
- 核心客户：民营产业园老板、股东。
- 核心业态：工业园区、厂房、仓库。
- 官网规划域名：`yizuw.org`；当前“登录系统”入口统一使用链接 `https://yizuw.cn`。
- 公司主体：东莞市宜租网络科技有限公司。公司主体仅作法定名称展示，不作为官网品牌名称使用。
- 交付方式：云端 SaaS 为主，同时支持云端私有化部署。
- 当前官网以 PC 管理后台为主要视觉展示；独立员工移动工作台仍缺少单独页面截图，因此不作为公开系统截图，但该视觉证据状态不降低已确认产品能力的完成状态。
- 官网不公开价格，不展示租户小程序。
- 2026-08-24 产品负责人确认 AI 招商匹配及统一数据源中的其他 AI 能力均已完善；官网以能力卡和人工复核流程说明对外展示，不伪造具体字段、按钮、匹配结果或可操作系统截图。
- 已确认可公开信息：联系电话 `18028231766`，联系地址“广东省东莞市高埗镇北王路高埗段5号”；公司主体、电话与地址必须通过统一站点配置维护。
- 企业微信显示名称为“瞰维智管官方客服”，认证主体说明为“宜租网络”。
- 六个园区均已正式使用瞰维智管，园区名称、照片、租户、车辆及图片相关公开展示授权已经取得；不得公开出租率、租金、欠费、收入等经营数据。

## 10. Stage 1：品牌视觉系统

| 变量 | 色值 | 用途 |
| --- | --- | --- |
| 品牌深蓝 | `#002359` | 品牌主色、深色区块 |
| 深蓝悬停 | `#001A42` | 深蓝交互态 |
| 品牌砖红 | `#9C3A2B` | 主行动按钮、少量强调 |
| 砖红悬停 | `#7F2D22` | 主行动按钮交互态 |
| 暖白背景 | `#F5F1E9` | 大面积柔和背景 |
| 正文深色 | `#172033` | 正文与标题 |
| 次级文字 | `#667085` | 辅助说明 |
| 分割线 | `#D8DEE8` | 边框与分隔 |
| 辅助数据蓝 | `#2D6CDF` | 数据与状态强调 |

- 白色与暖白约占 70%，深蓝约占 25%，砖红不超过 5%。
- 视觉方向为“东方可信感＋现代数字园区”。东方纹样低透明度、少量使用。
- 不采用红金节庆风、蓝紫霓虹科技风、重玻璃拟态。
- 使用统一 8px 间距体系；卡片圆角 12～16px；阴影克制。
- 桌面内容最大宽度控制在 1200～1280px；图标采用统一线性风格。
- 主按钮使用砖红，次按钮使用深蓝描边。
- 建立并实际使用颜色、字体、间距、圆角、阴影和响应式断点变量，以及按钮、标签、卡片基础组件。

## 11. Stage 1：首页内容与结构

### 11.1 顶部导航与首屏

- 顶部导航左侧为 Logo，中间为产品能力、解决方案、客户案例、关于我们，右侧为登录系统与预约演示。
- 导航吸顶；移动端使用可由键盘操作的折叠菜单并保留预约演示 CTA。
- 登录系统的 `href` 为 `https://yizuw.cn`，只做静态链接检查，不在测试中访问；预约演示进入 `/demo`。
- 首屏主标题：让工业园区少空置，让每一笔经营更清楚。
- 首屏副标题以已经完成本地截图溯源的经营总览、招商客户、合同、账单与报修工单为准，不扩大功能边界。
- 展示 PC 管理后台、云端 SaaS 与云端私有化部署；不展示独立员工移动端。
- 首屏右侧提供经营总览演示界面，仅覆盖截图可证明的累计应收、累计实收、待收余额、在租面积、经营回款脉搏、回款完成度和待办事项；所有数值显式标注为演示数据，不增加趋势图。

### 11.2 首页内容区

- 信任案例区标题为“真实园区，真实在用”，展示同富园区、佛山乐从园区、深圳坑梓园区、佛山九江园区、新塘西州、高埗同兴园区，不展示出租率、回款率或收入增长等指标。
- 老板关心的问题覆盖：经营总览、招商客户阶段、合同到期状态和报修工单状态。
- 招商客户列表与客户详情分开展示，不把多个页面拼成单一流程。
- 合同管理与账单管理分开展示，不推断截图无法证明的自动生成、审批、收款或核销流程。
- 十个已核验公开界面：园区经营总览、数据地图、设备总览、招商客户、客户详情、合同管理、账单管理、报修工单、人事总览、车辆出入管理。
- 待租厂房/可租房源列表与独立员工移动工作台保留内部 `UNVERIFIED`，官网公开内容中完全移除。
- 公开界面不再重复显示长段固定声明；继续保留“演示数据”标签，并在界面区说明真实页面来源与未核验模块边界。
- AI 独立能力区完整展示九项已完善 AI 能力，并明确“业务场景 → AI 处理 → 人员确认 → 结果写回系统”的人工判断边界；没有独立页面证据的 AI 能力不制作成模拟系统界面。
- 部署方式展示云端 SaaS、云端私有化部署和 PC 管理后台。
- 页尾 CTA 标题为“想先看看瞰维智管是否适合你的园区？”，包含预约演示、可点击拨号的真实电话和企业微信二维码。

## 12. Stage 1：Logo 与案例素材规则

- 原始素材只复制、读取并放入非公开 `assets/raw/`，不得覆盖或删除。
- 公开目录只保留合规优化后的派生素材，并移除 EXIF 与 GPS 信息。
- 页头 Logo 只显示图形和“瞰维智管”，不得显示小宣传语；页脚或品牌介绍区可使用带宣传语版本。宣传语保持“给园区管理装上大脑和翅膀，少操心，赚更多。”，不得重新设计 Logo 几何结构。
- 如果无法无损重制，不重新设计 Logo，使用清理后的透明 PNG，并记录“仍需人工矢量精修”。
- 园区图片只允许水平/透视校正、裁切、曝光与白平衡、轻微去雾、降噪、合理锐化及 WebP/AVIF 优化。
- 禁止增加或删除建筑、替换天空、生成车辆/人员/设施、AI 修改招牌、擦除水印、拉伸图片或把同一图片标为两个园区。
- 佛山九江园区含 `ndocn.com` 水印的图片不得进入公开资源；缺少无水印图时显示“图片待更新”。
- 新塘西州只使用 `incoming/` 中后来补发并经用户再次确认的替换图片；官网仅引用由该交接文件生成的 `xintang-xizhou-01.webp`，不引用旧第 9 张 raw 路径。
- 2026-08-20 已补充 Logo 位图原稿并完成透明 PNG 派生；已从桌面原图导入同富、佛山乐从、深圳坑梓和高埗同兴的公开 WebP 派生图。佛山九江水印图与新塘旧错误图只在非公开 raw 目录留档，禁止发布。
- 企业微信二维码必须从 `incoming/` 原始 PNG 复制到非公开 raw 目录，再将无损 PNG 派生文件用于公开页面；不得重新生成二维码内容。
- 2026-08-21 已核验企业微信原图为 496×391，新塘西州替换图为 800×533；二维码公开版仅裁切外部说明区域并保留完整静区，原图、裁切版及 128×128 显示尺寸均通过本地识别；新塘公开 WebP 为 800×500 且不含 EXIF。
- 2026-08-21 用户重新提供馆峰建筑照片并明确指定用于同富园区。该文件与原错误归入佛山乐从的 `source-05` 字节一致；官网将其重新归类为同富园区唯一展示图，并从佛山乐从展示中移除，避免同图多园区。
- 2026-08-21 用户重新提供佛山九江园区无水印图片。经目视和文件核验未发现 `ndocn.com` 水印；官网只使用该新原图的无 EXIF/GPS WebP 派生图，旧水印原图继续留在非公开 raw 目录且禁止发布。
- Stage 2 最新案例门禁覆盖上一条公开结论：佛山九江本阶段不进入案例列表、详情页、相关推荐或发布目录，所有九江原始资料只在非公开 raw 目录留档。

## 13. Stage 1：预约演示、SEO 与可访问性

- `/demo` 提供姓名、手机号、园区数量三个字段的表单 UI 与前端校验。
- 隐私政策复选框默认不选中；未同意不能提交；手机号按中国大陆 11 位手机号格式校验。
- 表单提交只改变本地界面状态，不请求、不保存、不上传数据；源码明确标注后续线索接口待安全与隐私评审后接入。
- 预约演示页面展示真实联系电话、联系地址与企业微信客服信息；表单成功态保持本地状态，不向生产接口发送信息。
- 保持 `noindex, nofollow`；完善首页 title、description、Canonical、Open Graph 和基础 Twitter Card。
- 页面保持单一 H1、正确标题层级、语义化 HTML、替代文本、键盘可操作导航、明显焦点状态、足够对比度和规范内部链接。
- 禁止关键词堆砌、虚构经营数据/资质/历史和远程热链图片。

## 14. Stage 1：测试、边界与版本控制

- 必须通过 `npm run lint`、`npm run build`、本地启动和七个路由 HTTP 状态检查。
- 必须检查 1440px、1024px、768px、390px 视口，无横向溢出，导航、CTA 与移动菜单可用。
- 静态检查必须确认：系统入口链接正确且未实际访问、AI 能力没有虚构操作界面、未把法定主体简称为官网品牌、无真实敏感经营数据、无 TypeScript 源文件、无违禁水印图、无重复旧第 9 张、预约表单只调用官网独立 API、阶段环境保持 noindex。
- 如浏览器环境支持，保存 1440px PC 和 390px 移动端首页全页截图并完成视觉复核。
- 除 Stage 1.1 经用户明确授权的只读溯源审计外，不访问或登录 `yizuw.cn`；任何情况下均不得执行生产写操作。
- 不修改 DNS，不部署，不推送远程仓库。
- Stage 1.1 与其他全部硬性验收通过后创建本地提交：`feat: build kwzg brand system and verified homepage`。

## 15. Stage 1.1：真实系统功能与官网界面溯源审计

### 15.1 最高优先级约束

- 官网所有产品界面展示必须来源于当前真实系统 `https://yizuw.cn`。
- 允许对真实页面进行高保真视觉重设计，但不允许创造系统中不存在的功能、字段、按钮、状态、图表、数据关系、权限关系、业务计算或业务流程。
- 每张官网系统展示图必须与一个真实系统页面对应；不得把多个系统页面拼接后冒充一个页面。
- 如需组合展示多个能力，必须拆分为多个独立页面卡片并分别记录来源。
- 只有在 `docs/system-ui-source-matrix.md` 中审核为 PASS 的展示界面才允许进入官网。

### 15.2 只读访问权限与禁区

- 用户授权本轮只读打开页面、查看菜单、字段、表格、卡片、图表、筛选条件、导航关系并记录真实功能。
- 禁止新增、编辑、删除、提交表单、审批、收款、核销、结转、财务处理，以及操作设备、门禁、访客、车辆、工单、角色、账号、权限或系统配置。
- 禁止自动化批量请求和压力测试；访问保持单页、人工节奏。
- 如出现登录页，必须暂停并由用户本人手动登录；不得要求用户把密码粘贴到代码、项目文件或对话中。
- 真实账号、密码、客户数据、租金、欠费、合同、手机号等生产信息不得进入代码、文档、终端历史、公开资源或 Git。
- 本地脱敏参考截图只能进入 `private-reference/yizuw-ui/` 等 Git 忽略目录，不得进入 `public`、`assets` 或提交；未脱敏原图完成复核后删除。

### 15.3 溯源范围与判定

- 重点核验经营总览、园区与空置厂房、招商管理、合同与财务、设备管理、运维管理、车辆与访客、员工与组织。
- 现有界面必须逐一核验；当前可公开展示的十个真实来源为园区经营总览、数据地图、设备总览、招商客户、客户详情、合同管理、账单管理、报修工单、人事总览、车辆出入管理。
- 每项必须记录官网位置、展示名称、真实菜单、真实页面或路由、字段、按钮、状态、数据关系、视觉美化内容、是否新增不存在内容和 PASS/FAIL 结论。
- 只有菜单、空容器或未完成页面的能力不得作为成熟功能展示。
- 无法找到真实来源的页面必须从官网展示中删除；字段、按钮、状态或流程不准确时必须修正后复核。
- 展示使用脱敏且逻辑自洽的演示数据，不直接复制生产数据截图。
- 每张系统展示保留明确的“演示数据”标识，不重复显示长段固定声明。

### 15.4 AI 能力边界

- AI 招商匹配不得与已核实系统界面混用状态标签或伪装为截图页面。
- 在补齐独立系统页面证据前，不得放进已核实系统界面组，不得制作具体可操作页面，不展示虚构字段、按钮、匹配结果或执行记录。
- AI 经营问数、自动催缴、RAG 客服和合同智能审查第一版不作为已上线能力展示。

### 15.5 Stage 1 提交门禁

Stage 1 历史提交已完成；2026-08-24 最新完整能力确认后，公开页面继续满足系统截图单页一源、公开无证据系统界面为 0、原始生产截图不进入发布目录、AI 不伪装为系统截图、生产数据不进入代码和 Git、对 `yizuw.cn` 的生产写操作数量为 0。

### 15.6 本地截图审计执行结果

- 2026-08-21 本轮只读取 `private-reference/yizuw-ui/` 中用户已保存并完成遮盖的 10 张 PNG 与采集清单，不再使用 Browser、Chrome 或 Computer Use，不再访问 `yizuw.cn`。
- 可辨识功能菜单为 32 项（不计分组标题）；实际核对页面 8 个，其中 6 个进入官网公开重构，2 个保持内部 `UNVERIFIED`。
- `docs/system-ui-source-matrix.md` 记录字段名和功能结论，不抄录截图中的客户资料、电话、金额、合同编号或员工信息。
- 公开生产截图数量为 0；全部系统展示由 Vue、JavaScript 与 CSS 使用演示数据重构。

## 16. Stage 2 追加硬门禁：A+B 全站动效系统

### 16.1 范围与总体方向

- 本节覆盖并追加到原 Stage 2，不改变既定数据范围：预约信息只保存到官网独立数据库供官网后台查看；不发送企业微信通知；不连接、不调用、不修改 `yizuw.cn`。
- Stage 2 最终仍只允许创建一个功能提交，动效不得拆成单独提交。
- 采用 A+B 混合方案：整体高级、稳重、克制，保持东方科技企业风；首页、产品、解决方案允许较明显科技动效，案例、关于、预约、隐私使用柔和内容动效，后台只使用简洁状态反馈。
- 动效继续使用深蓝、砖红、暖白体系；禁止廉价霓虹、剧烈弹跳、快速旋转、大面积闪烁和满屏持续运动。
- “所有页面具有动态体验”指每页具备进入、滚动、交互和操作反馈，不要求元素持续运动。

### 16.2 集中式动效架构

- 集中定义持续时间、缓动曲线、位移、透明度和层级变量，不在组件中堆积无规范的零散动画。
- 建立可复用滚动出现组件或指令，以 `IntersectionObserver` 触发，不持续监听滚动位置。
- 页面切换、卡片出现、按钮反馈、图片加载和表单状态使用统一规范；优先动画 `transform` 与 `opacity`，不得导致布局抖动。
- SSR 与客户端激活必须稳定，Hydration 错误为 0；JavaScript 失效或动画关闭时，正文、导航、表单和联系方式仍完整可见可用。
- 首屏标题、主要 CTA 与核心内容不得等待观察器或动画才能显示。
- 页面截图与自动化测试必须提供稳定的关闭动效模式，禁止随机粒子造成截图漂移。

### 16.3 公共与路由专属动效

- 七个公开页面必须具备：页面进入/离开、内容滚动渐入、按钮/链接/卡片悬停按下及键盘焦点反馈、至少一种内容匹配的专属动效、页头滚动态与当前导航提示、移动菜单过渡、页脚轻量进入。
- `/`：允许缓慢深蓝渐变、东方纹样网格、轻量数据流光及桌面端低透明度光点；标题、说明、CTA、经营总览分层进入。经营总览只能动画已核实卡片、固定演示数字和状态视觉，不得制造实时监控、实时预警或循环变化数据。
- `/products`：已核实功能分组出现，切换界面使用淡入、位移或遮罩；只动画已有卡片、列表、标签和按钮。AI 招商匹配不得伪装为可操作的系统截图界面。
- `/solutions`：场景卡片分批出现，可使用低调线条连接和当前场景突出，不新增未经核实的流程、步骤或系统能力。
- `/cases`：园区图片平滑加载、轻微缩放和画廊切换；桌面端可小幅视差，移动端关闭。悬停只显示园区名称和查看提示，不增加出租率、收入、合作时间或客户评价。
- `/about`：品牌标志、宣传语、公司信息依次进入；电话、地址、企业微信二维码使用轻量反馈，不增加成立年份、团队规模、资质、专利或发展历程。
- `/demo`：输入聚焦、错误、隐私确认、提交中、成功和失败均有文字与动画反馈；按钮必须防重复提交，数据库失败不得显示成功动画。
- `/privacy`：章节克制渐入和当前阅读位置提示；不使用粒子、强视差或大面积背景动画。
- `/admin/login`、`/admin/leads`：只为登录、筛选、详情抽屉、状态更新、备注保存和错误提供克制反馈；不制作虚构统计大屏、图表或实时线索动画，不通过动效暴露完整手机号或凭证。

### 16.4 科技背景、降级与性能

- 首页、产品、解决方案优先使用 CSS 或轻量 Canvas；默认不引入 Three.js、重型粒子库或大型动画依赖。
- 粒子数量和速度必须克制；移动端、低性能设备、组件离开视口、标签隐藏或用户启用减少动态时，暂停或关闭粒子、强视差和复杂光效，保留静态渐变。
- 完整支持 `prefers-reduced-motion: reduce`：关闭粒子、视差、数字滚动和非必要位移，保留必要状态文字、即时反馈、键盘焦点和屏幕阅读器语义。
- 动效不得造成横向溢出、文本模糊、按钮不可点击、首屏明显变慢或滚动卡顿。

### 16.5 真实性边界

- 系统界面动效只覆盖已经通过截图矩阵核验的园区经营总览、数据地图、设备总览、园区管理、招商客户、客户详情、合同管理、账单管理、报修工单、人事总览和车辆出入管理。
- 不公开待租厂房列表、房态矩阵、独立员工移动工作台；不把报修工单包装成设备巡检。
- 不制作虚构趋势图、招商看板、实时数字变化、虚构字段、数据、按钮、状态、流转或业务能力。
- 所有系统界面继续显示“演示数据”标识，并通过展示区文案说明溯源边界；不重复显示长段固定声明。

### 16.6 文档与提交门禁

- 必须创建 `docs/motion-system.md`，记录原则、各路由动效、桌面/移动降级、减少动态处理、持续动画暂停、真实性边界和性能检查结果。
- 必须验证七个公开页面、后台登录与线索管理均有有意义的动态体验；1440、1024、768、390 无横向溢出；移动菜单、案例画廊、表单状态、后台抽屉、减少动态和关闭动效模式正常。
- lint、build、测试、路由、数据库和安全验收必须继续全部通过；不得访问或写入 `yizuw.cn`，不得部署、推送、修改 DNS 或操作生产数据。
- 最终报告必须包含：`MOTION_SYSTEM`、`PUBLIC_ROUTES_WITH_MOTION`、`ADMIN_MOTION`、`REDUCED_MOTION`、`MOBILE_MOTION_FALLBACK`、`ANIMATION_PERFORMANCE`、`HYDRATION_ERRORS`、`HORIZONTAL_OVERFLOW`、`FABRICATED_SYSTEM_UI`、`PRODUCTION_WRITE_ACTIONS`。
- 任一项失败，不得创建 Stage 2 功能提交。

## 17. Stage 2 追加硬门禁：客户案例列表与独立详情页

- `/cases` 调整为五案例列表与本地地区筛选；地区为全部案例、东莞、佛山、深圳、广州。
- 只公开同富园区、佛山乐从园区、深圳坑梓园区、新塘西州、高埗同兴园区，并分别使用 `/cases/[slug]` 独立详情页。
- 案例数据统一维护在 `app/data/cases.js`，页面复用筛选、卡片、画廊、功能展示、案例导航和预约 CTA 组件，不建立案例数据库或内容管理后台。
- 每个详情页必须具有面包屑、主视觉、案例应用概览、差异化管理场景、相关功能展示、定性管理价值、授权实景画廊、上下案例导航与预约演示入口。
- 相关功能只能从园区经营总览、招商客户、客户详情、合同管理、账单管理、报修工单中选择；标题使用“相关功能展示”，不得声称某园区实际部署了具体模块。
- 每个系统界面继续对应溯源矩阵中的单一真实页面，使用 Vue、JavaScript、CSS 与演示数据重构，不使用生产截图或真实客户数据。
- 不得编造客户证言、负责人发言、园区面积、楼栋或租户数量、出租率、租金、欠费、收入、上线时间、实施周期、ROI 或任何提升比例。
- 佛山九江不进入案例列表、详情页、相关推荐和发布目录；相关原图只在非公开 raw 目录保留。
- 案例页纳入 A+B 动效系统：列表筛选与卡片错峰出现，详情首屏与章节分层进入、阅读进度、桌面轻视差、画廊和界面切换；移动端和减少动态模式关闭非必要视差与位移。
- 每个详情页具有独立标题、描述、canonical、唯一 H1、正确标题层级、实景分享图、图片替代文本和键盘可操作画廊；阶段环境继续保持 `noindex, nofollow`。

## 18. Stage 2 追加硬门禁：全站右侧悬浮客服栏

- 默认布局统一挂载 `FloatingContactBar.vue`，覆盖七个公开主页面与五个案例详情页共 12 个公开路由；所有 `/admin` 前缀页面默认隐藏，打印样式隐藏。
- 联系方式只读取 `app/config/site.js`：电话 `18028231766`、链接 `tel:18028231766`、公开拨号引导页 `https://yizuw.org/call`、企业微信二维码 `/assets/wecom-qrcode/kwzg-wecom-qr.png`、电话二维码 `/assets/contact/kwzg-phone-call-qr.png`。
- 微信不会把 `tel:` 二维码作为拨号操作执行，电话二维码因此必须离线编码公开 HTTPS 拨号引导页 `https://yizuw.org/call`；引导页在微信内自动尝试唤起拨号，并始终保留真实 `<a href="tel:18028231766">` 按钮作为用户确认与平台拦截时的兜底。
- 电话二维码必须为黑码白底、至少四模块静区、不叠加 Logo 或装饰、不包含 EXIF、GPS 或文本元数据；原始 512px、页面 168px、160px 与 128px 均须机器识别通过。
- 桌面端提供联系客服、电话咨询、返回顶部三个入口；联系与电话面板支持悬停、点击固定、互斥展开、150ms 移动容错、点击空白处、Esc 和路由变化关闭。
- 移动端联系客服使用带遮罩、焦点管理和背景滚动锁定的对话框；电话咨询必须使用真实 `<a href="tel:18028231766">` 直接调用系统拨号界面，不显示电话二维码。
- 返回顶部超过 520px 后出现，显示轻量真实滚动进度；普通模式平滑返回，减少动态或关闭动效模式即时返回，并将焦点移至主要内容。
- 全部入口使用真实按钮或链接、明确 ARIA 名称、`aria-expanded` 与 `aria-controls`；移动对话框支持焦点循环、Esc、关闭后焦点回归，触摸目标不少于 44px。
- 组件不写数据库、Cookie 或 LocalStorage，不收集手机号，不调用第三方客服、远程二维码、电话服务或 `yizuw.cn`，不发送行为日志。
- 1440、1024、768、390 四档必须无横向溢出、面板完整位于视口内且不遮挡可见主要 CTA；控制台与 Hydration 错误均为 0。

## 19. Stage 2 追加硬门禁：科技型中小企业资质展示

- 资质属于东莞市宜租网络科技有限公司，准确名称为“科技型中小企业”，入库登记编号为 `2026441901A0001155`，监督机构为广东省科学技术厅，登记日期为 2026年8月11日。
- 公开表达仅限“东莞市宜租网络科技有限公司已完成科技型中小企业入库登记”，不得暗示瞰维智管产品获得政府认证、指定、推荐或国家级荣誉。
- 用户提供原图为 `incoming/tech-sme-2026-original.jpg`；原样归档至 `assets/raw/certifications/tech-sme-2026-original.jpg`，公开页面不得直接引用 raw JPEG。
- 公开图仅使用 `public/assets/certifications/tech-sme-2026-cover.webp` 与 `public/assets/certifications/tech-sme-2026-detail.webp`，只允许裁切、透视/倾斜校正、曝光、白平衡、对比度和合理锐化，并清除 EXIF、GPS 与无关元数据。
- 图片处理不得使用生成式 AI、扩图、重绘、文字替换、徽标替换、印章增删或虚假细节；原有资质名称、企业名称、编号、机构、日期与标识必须保持不变。
- 资质事实只维护在 `app/config/site.js`；首页和关于页复用 `QualificationSection.vue`，不得分别硬编码，不加入预约数据库或后台。
- 首页资质板块位于客户案例之后、最终预约 CTA 之前；关于页完整资质板块位于公司介绍之后、联系方式之前。
- 大图弹窗使用真实按钮，支持遮罩、关闭按钮、Esc、焦点循环、关闭后焦点回归、背景滚动锁定、背景屏幕阅读器隐藏与路由切换关闭，层级高于悬浮客服栏。
- 资质板块只使用低饱和金色细边与一次性流光，不制作奖状墙、官方认证印章、奖杯、皇冠或持续闪光；`prefers-reduced-motion` 与 `?motion=off` 关闭缩放、流光和非必要位移。
- 首页与关于页继续保持 `noindex, nofollow`；不增加虚假资质结构化数据、远程图片或外部资质查询请求。
- 1440、1024、768、390 四档须无横向溢出，编号不撑破卡片，图片不拉伸或裁掉关键文字，弹窗和关闭按钮在视口内，并避开悬浮客服栏及预约 CTA。

## 20. Stage 2 追加硬门禁：顶部首页导航

- 桌面与移动端主导航共用 `SiteHeader.vue` 内的单一数据源，顺序固定为：首页 `/`、产品能力 `/products`、解决方案 `/solutions`、客户案例 `/cases`、关于我们 `/about`；登录系统与预约演示继续位于右侧且保持既有行为。
- 首页必须使用 Nuxt 内部路由并严格匹配 `route.path === '/'`；不得硬编码本地端口、外部官网 URL、`window.location` 或新窗口。产品、解决方案和关于页激活对应栏目；案例列表与所有 `/cases/` 子路由激活客户案例；预约与隐私页面不错误激活主栏目。
- 当前栏目同时使用文字字重和品牌色标记，不只依赖颜色；链接设置 `aria-current="page"`，支持键盘焦点、减少动态和关闭动效模式。
- 从任意公开页面点击首页返回 `/` 并回到顶部，移动菜单立即关闭；已经位于首页时再次点击不重复加载，普通模式平滑回顶、减少动态时立即回顶。
- 页头紧凑 Logo 的图标与品牌名整体包含在 `<NuxtLink to="/">` 中，ARIA 名称为“瞰维智管官网首页”，不显示宣传语、不改变比例或清晰度。
- 案例详情面包屑使用语义化 `nav` 和有序列表；首页链接 `/`、客户案例链接 `/cases`，当前案例只显示文本并设置 `aria-current="page"`，不得使用 history back 代替。
- 移动菜单首页位于第一项，支持点击关闭、Esc 关闭、页头外点击关闭、焦点回归和正确的 `aria-expanded`；390px 下全部链接可点击，悬浮客服栏不受影响。
- 1440、1024、768、390 四档必须无导航换行、文字重叠、Logo 变形、CTA 越界或横向溢出；控制台错误、Hydration 错误、首页非首页误高亮和多重激活数量均为 0。
- 导航是官网功能，不加入 `docs/system-ui-source-matrix.md`；不得产生任何 `yizuw.cn` 请求或生产写操作。

## 21. Stage 2 电话咨询修正：微信兼容拨号中转页

- 正式模式固定为 `PHONE_QR_MODE=HTTPS_CALL_LANDING_PAGE`；电话二维码内容严格为 `https://yizuw.org/call`，不得使用 `tel:` 作为二维码内容，不带查询、追踪、Token、短链或第三方跳转。
- `app/config/site.js` 是官网地址、电话、`tel:` 链接、`/call` 路径、绝对拨号页 URL 和二维码素材路径的唯一事实来源；组件、页脚、关于页、SEO 和业务代码不得建立不同副本。
- 已能解码为正确 HTTPS URL 的 512×512 PNG 必须保留，不重复生成；原图、160px 和 128px 均须严格解码为同一 URL，并保持黑码白底、四模块静区、无 Logo、渐变或元数据。
- `/call` 使用无主导航、复杂页脚、悬浮客服栏、案例图、系统演示、粒子和后台内容的极轻量独立页面；必须支持直接访问、刷新、预渲染或 Node Server，且返回 HTTP 200。
- 页面显示紧凑 Logo、“电话咨询”、拨号状态、`18028231766`、始终可见的真实 `<a href="tel:18028231766">立即拨打</a>`、返回官网和系统确认说明。
- 客户端只在微信或移动 User-Agent 上自动尝试一次 `tel:`；桌面不自动尝试。自动拨号是渐进增强，不能循环、记录结果、调用 API、写数据库/Cookie/LocalStorage，也不能声称拨号成功。
- 桌面电话弹窗显示“微信扫码拨打电话”及“扫码后打开拨号页面，请按手机提示确认拨打。”；开发模式可以渲染尚未部署的提示，生产 DOM 不得包含该提示。
- 移动端悬浮电话继续直接链接 `tel:18028231766`，不得让用户扫描本机二维码或绕行第三方页面。
- `/call` 永久 `noindex, nofollow`，标题固定为“电话咨询｜瞰维智管”，描述固定为“拨打瞰维智管电话18028231766，咨询园区管理数字化解决方案。”；不收集扫码来源、设备或手机号，不使用统计、电话或短信服务。
- 本地必须验证四档响应式、移动 UA 与桌面路径、一次自动尝试上限、永久备用按钮、减少动态、键盘、无横向溢出、无控制台或 Hydration 错误，以及 0 数据库写入、0 外部电话服务和 0 生产系统请求。
- 微信当前“网络出错”是正式 `yizuw.org/call` 尚未部署导致；本 Stage 2 不部署，因此公开页面、iPhone 微信和 Android 微信真机状态必须记录为 `DEFERRED_NOT_DEPLOYED`，不得伪造 `PASS`。
- 正式部署后再验收首页与 `/call` 的 200、HTTPS 证书、重定向、iPhone/Android 微信与系统相机扫码、拨号确认及备用按钮；部署前不得改变 DNS、服务器或公开状态。

## 22. Stage 2 紧急修复：预约表单数据库闭环

- 预约信息唯一保存位置为官网独立 MariaDB/MySQL，后台入口为 `/admin/leads`；不得改用浏览器存储、JSON 文件、内存、通知系统或 `yizuw.cn`。
- 公开接口固定为 `POST /api/demo-requests`，必须执行 Content-Type、请求体大小、同源、字段白名单、服务端字段、蜜罐、最短填写时间、限流、幂等和数据库事务校验。
- 线索与初始审计必须在同一事务中写入；初始状态为“待跟进”，两项全部成功后才允许返回 HTTP 201 和显示成功态。
- 前端提交期间必须禁用按钮并显示“提交中…”；成功文案固定为“预约提交成功 / 我们已收到您的预约信息，工作人员将尽快与您联系。”，不得声称已经发送任何通知。
- 字段错误显示在对应字段；429、503、网络失败、重复和未知错误使用不同且不泄露内部信息的文案，错误状态必须具有可访问文字提示。
- 数据库连接、配置和迁移类故障使用稳定 503 错误码；未知错误不得统一误判为数据库错误，浏览器响应不得包含 SQL、主机、表结构、堆栈、文件路径或凭证。
- 只信任显式配置的反向代理 IP；未配置可信代理时不得采用用户提供的 `X-Forwarded-For` 作为限流地址。
- `.env`、本地数据库、测试日志和私有验收脚本不得进入 Git；`.env.example` 只能记录无敏感信息的变量名与占位符。
- 必须在独立测试数据库验证正常写入、双击与幂等、非法输入、限流、非法 Origin、请求体超限、SQL/XSS、初始审计失败回滚、数据库停机 503 与恢复写入。
- 后台必须能搜索并查看新预约，列表手机号保持脱敏，状态与日期筛选正常，初始审计存在；日志中完整手机号出现次数必须为 0。
- 必须通过 Node Server 生产式本地构建、1440/1024/768/390 响应式、减少动态、lint、build、完整测试和 Git 安全门禁；不得访问或修改生产系统，不推送、不部署、不修改 DNS。

## 23. 完整能力官网整改与状态分层（最新要求）

本节覆盖本文档中“官网公开能力只能限制为少量页面”“其他业务板块整体隐藏”的历史限制，但不覆盖真实性、隐私、安全、生产隔离和经营数据保护要求。

- 官网完整呈现经营驾驶舱、资产租赁、招商客户、合同、账单财务、物业维护、设备能耗、人事审批、门禁通行、AI、权限部署和软硬件接入。
- 每项一级能力与子功能统一从 `app/data/product-capabilities.js` 读取，公开完成状态统一为 `available`，并单独标注标准、项目配置、接口与设备实施、云端私有化部署四类交付方式。
- 有真实页面证据的功能可以用 Vue/CSS 与演示数据重构公开界面；仅有菜单、规划或硬件接入依据的能力使用文字、状态卡和关系图，不制作成虚假后台。
- 首页核心表达为“让园区经营，从靠人盯，变成系统协同”，使用“帮助、支持、推动、减少、加快、提升”等非保证性价值表达。
- 产品页按十二个能力板块完整展开；解决方案页同时按园区类型和岗位角色组织。
- AI 招商匹配及其他八项 AI 能力均按产品负责人确认作为已完善能力展示；没有独立页面证据时使用文字与流程说明，不展示虚构操作界面。
- 智能水电表、门禁、视频、能耗、现有设备和第三方接口作为已完善接入能力展示，交付方式为接口与设备实施；不承诺任何品牌一定可以接入。
- 云端私有化部署、指定服务器、数据库和网络环境按项目评估；SaaS、权限、备份与恢复分别说明责任边界。
- 客户案例继续只使用五个已授权园区，不增加经营数字、提升百分比、客户引语或虚构上线日期。
- 预约演示继续使用 `POST /api/demo-requests` 和官网独立数据库，不写入或调用 `yizuw.cn`。
- 继续保持 `noindex, nofollow`，不部署、不推送、不修改 DNS。
